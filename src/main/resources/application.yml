server:
  # 端口
  port: 8761
spring:
  application:
    #  服务名
    name: eureka
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/